name: Sync Files
on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
      - name: Run GitHub File Sync
        uses: BetaHuhn/repo-file-sync-action@e2715cb544edee3eff1aae1efbca9be0136e276f
        with:
          Token: ${{ secrets.SHARED_GITHUB_ACTIONS }}
